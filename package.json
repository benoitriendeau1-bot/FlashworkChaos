{
  "name": "flashwork-chaos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "plan": "node src/cli.mjs --dry-run",
    "start": "node src/cli.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
